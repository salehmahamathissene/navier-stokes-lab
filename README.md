# navier-stokes-lab

Educational 2D incompressible Navier–Stokes solver on a periodic square using
a projection method and FFT Poisson solve.

## Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .

